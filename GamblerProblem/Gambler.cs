using System;
using System.Collections.Generic;
using System.Text;

namespace GamblerProblem
{
    class Gambler
    {
        public void Gambling()
        {
            const int gameStake = 100;
            const int gameBet = 1;

            Console.WriteLine(" Initial values : \t Stake = ${0} \t Bet per game = {1}", gameStake,gameBet);
        }
    }
}
